server:
  port: 3000
  

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/admin_panel
    username: root
    password: root
  application:
    name: admin-panel     
  
  jpa:
    hibernate:
      ddl-auto: update  
    show-sql: true